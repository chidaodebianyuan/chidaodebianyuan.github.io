+++
date = '2026-03-09T12:54:28+08:00'
draft = false
title = '推理优化'
math = true
+++

# Self-Attention

## 机理

将不同输入间的 Query 和 Key 通过某种计算得到不同输入间的相关性，这个相关性用注意力分数表示。用注意力分数对不同输入的 Value 进行加权求和，得到最终输出。

$$ a = \softmax\left(\frac{Q K^T}{d_k}\right) V $$

除以 $d_k$ 的过程是点乘自注意力机制名称中的 Scale 过程。因为 $Q K^T$ 值的大小常与矩阵维度有关，维度越大结果越大，而 Softmax 函数的输入越大，则输出越接近函数的平滑段，容易造成梯度消失。故采用上述 Scale 计算进行缩放，解决梯度消失的问题。

## 示例

输入序列为“我是谁”，每个字可以表达为一个 1×4 的向量，记为 $X_1, X_2, X_3$

总的输入 $X = [X_1 X_2 X_3]$

这里看“我”和“是”的注意力分数

$$ q_1 = X_1 W_Q, \quad q_2 = X_2 W_Q $$
$$ k_1 = X_1 W_K, \quad k_2 = X_2 W_K $$
$$ v_1 = X_1 W_V, \quad v_2 = X_2 W_V $$
$$ W_Q, W_K, W_V \in \mathbb{R}^{d_x \times d_k} $$

$$ \score_{11} = \frac{q_1 \cdot k_1}{d_k}, \quad \score_{12} = \frac{q_1 \cdot k_2}{d_k} $$
$$ \score_{21} = \frac{q_2 \cdot k_1}{d_k}, \quad \score_{22} = \frac{q_2 \cdot k_2}{d_k} $$

$$ \score_{11} = \frac{e^{\score_{11}}}{e^{\score_{11}} + e^{\score_{12}}} $$
$$ \score_{12} = \frac{e^{\score_{12}}}{e^{\score_{11}} + e^{\score_{12}}} $$
$$ \score_{21} = \frac{e^{\score_{21}}}{e^{\score_{21}} + e^{\score_{22}}} $$
$$ \score_{22} = \frac{e^{\score_{22}}}{e^{\score_{21}} + e^{\score_{22}}} $$

$$ z_1 = v_1 \times \score_{11} + v_2 \times \score_{12} $$
$$ z_2 = v_1 \times \score_{21} + v_2 \times \score_{22} $$

实际的代码实现中，Self Attention 的计算过程是使用矩阵快速计算的，一次就得到所有位置的输出向量。

### 代码

```python
import torch
import torch.nn as nn

class Attention(nn.Module):
    def __init__(self, input_dim, attention_dim):
        super(Attention, self).__init__()
        self.input_dim = input_dim
        self.attention_dim = attention_dim
        
        # 创建一个线性变换层用于查询、键和值的表示
        self.linear_query = nn.Linear(input_dim, attention_dim)
        self.linear_key = nn.Linear(input_dim, attention_dim)
        self.linear_value = nn.Linear(input_dim, attention_dim)

        # 创建一个softmax层用于计算注意力权重
        self.softmax = nn.Softmax(dim=1)

    def forward(self, queries, keys, values):
        # 将输入通过线性变换层
        query = self.linear_query(queries)
        key = self.linear_key(keys)
        value = self.linear_value(values)

        # 计算注意力权重
        scores = torch.bmm(query, key.transpose(1, 2))

        attention_weights = self.softmax(scores)

        # 使用注意力权重加权求和值向量
        output = torch.bmm(attention_weights, value)

        return output, attention_weights
```

# MHA

Self-Attention 虽然能让模型获得良好的长程相关性，但在自然语言处理中，很多时候对单个词语的翻译需要参考多个不同的其他词语，也使注意力不能只集中在一处。当 Q 和 K 序列相同时，注意力机制会为上下文中的相同单词分配非常大的分数，而在实践中，相关词往往比相同词更重要。多头注意力机制的提出能够很好地解决这个问题。多头注意力机制类似于卷积神经网络中的多个不同卷积核，使注意力层的输出能够表达不同子空间中的注意力信息，从而增强模型的表达能力。

这种机制从如下两个方面增强了 attention 层的能力：

- 它扩展了模型关注不同位置的能力。在上面的例子中，第一个位置的输出 $z_1$ 包含了句子中其他每个位置的很小一部分信息，但 $z_1$ 仅仅是单个向量，所以可能仅由第1个位置的信息主导了。当我们翻译句子：The animal didn’t cross the street because it was too tired 时，我们不仅希望模型关注到 "it" 本身，还希望模型关注到 "The" 和 "animal"，甚至关注到 "tired"。这时，多头注意力机制会有帮助。

- 多头注意力机制赋予 attention 层多个“子表示空间”。多头注意力机制会有多组 $W_Q, W_K, W_V$ 的权重矩阵（在 Transformer 的论文中，使用了 8 组注意力），因此可以将 X 变换到更多种子空间进行表示。每一组注意力的权重矩阵都是随机初始化的，但经过训练之后，每一组注意力的权重 $W_Q, W_K, W_V$ 可以把输入的向量映射到一个对应的”子表示空间“。

多头注意力机制其实是将原始的输入序列进行多组自注意力计算，再将得到的输入直接进行堆叠。

## 示例代码

```python
import torch
import torch.nn as nn

class MultiHeadAttention(nn.Module):
    def __init__(self, dim_input, d_model, num_heads):
        super(MultiHeadAttention, self).__init__()
        self.num_heads = num_heads
        self.d_model = d_model
        self.d_k = d_model // num_heads  # 每个头的维度
        self.query_linear = nn.Linear(dim_input, d_model)  
        self.key_linear = nn.Linear(dim_input, d_model)  
        self.value_linear = nn.Linear(dim_input, d_model)  
        self.output_linear = nn.Linear(d_model, d_model)  

    def forward(self, input):
        batch_size = input.size(0)
        seq_len = input.size(1)
        
        # 对输入进行线性变换
        query = self.query_linear(input)
        key = self.key_linear(input)
        value = self.value_linear(input)

        # 将变换后的张量重塑为适合多头注意力的形式
        query = query.view(batch_size, seq_len, self.num_heads, self.d_k)
        key = key.view(batch_size, seq_len, self.num_heads, self.d_k)
        value = value.view(batch_size, seq_len, self.num_heads, self.d_k)

        # 交换键和值张量的第二个和第三个维度，以便进行矩阵乘法
        query = query.transpose(1, 2)  # [batch_size, num_heads, seq_len, d_k]
        key = key.transpose(1, 2)  # [batch_size, num_heads, seq_len, d_k]
        value = value.transpose(1, 2)  # [batch_size, num_heads, seq_len, d_k]

        # 计算注意力得分（使用缩放点积注意力）
        scores = torch.matmul(query, key.transpose(-2, -1))  # [batch_size, num_heads, seq_len, seq_len]
        scores = scores / torch.sqrt(torch.tensor(self.d_k, dtype=torch.float32))  # 缩放得分

        # 应用softmax函数得到注意力权重
        attention_weights = nn.Softmax(dim=-1)(scores)

        # 使用注意力权重加权求和值向量
        attention_output = torch.matmul(attention_weights, value)  # [batch_size, num_heads, seq_len, d_k]

        # 将注意力输出重新排列并连接，恢复到原始维度
        attention_output = attention_output.transpose(1, 2).contiguous().view(batch_size, seq_len, self.d_model)

        # 最后，通过一个线性变换将注意力输出转换回原始维度
        attention_output = self.output_linear(attention_output)

        return attention_output

# 使用示例
d_model = 256  # 输入维度
num_heads = 8  # 注意力头数

# 创建Multi-Head Attention层
attention = MultiHeadAttention(d_model, num_heads)

# 创建输入张量
batch_size = 4
seq_len = 10
input = torch.randn(batch_size, seq_len, d_model)

# 前向传播
output = attention(input)
```

对于训练来说，时间长是可以忍受的，但推理时间长是不能接受的。如何提高吞吐率是很关键的一点。以下几点是值得去思考的。

KV Cache 的占用：$batch\_size \times seq\_len \times dim \times num\_layer$

相同的算力更高吞吐率（YOCO）

相同的算力更大的模型 (MOE)

相同的模型更强大的推理能力 (KAN)

# KV Cache

观察一下基于 Decoder 架构的大模型的生成过程。假设模型只是一层 Self Attention，用户输入“中国的首都”，模型续写得到的输出为“是北京”，模型的生成过程如下：

将“中国的首都”输入模型，得到每个 token 的注意力表示（绿色部分）。使用“首都”的注意力表示，预测得到下一个 token 为“是”（实际还需要将该注意力表示映射成概率分布 logits，为了方便叙述，我们忽略该步骤）。

将“是”拼接到原来的输入，得到“中国的首都是”，将其输入模型，得到注意力表示，使用“是”的注意力表示，预测得到下一个 token 为“北”。

将“北”拼接到原来的输入，依此类推，预测得到“京”，最终得到“中国的首都是北京”

在每一步生成中，仅使用输入序列中的最后一个 token 的注意力表示，即可预测出下一个 token。但模型还是并行计算了所有 token 的注意力表示，其中产生了大量冗余的计算（包含 qkv 映射，attention 计算等），并且输入的长度越长，产生的冗余计算量越大。例如：

在第一步中，我们仅需使用“首都”的注意力表示，即可预测得到“是”，但模型仍然会并行计算出“中国”，“的”这两个 token 的注意力表示。

在第二步中，我们仅需使用“是”的注意力表示，即可预测得到“北”，但模型仍然会并行计算“中国”，“的”，“首都”这三个 token 的注意力表示。

在推理阶段，当输入长度为 n，我们仅需使用 $z_n$ 即可预测出下一个 token，但模型却会并行计算出 $z_1, z_2, \dots, z_n$，这部分会产生大量的冗余计算。而实际上 $z_n$ 可直接通过公式 $z_n = \softmax(\frac{q_n K^T}{d_k}) V$ 算出。

KV Cache 的本质是以空间换时间，它将历史输入的 token 的 K 和 V 缓存下来，避免每步生成都重新计算历史 token 的 K 和 V 以及注意力表示 z，而是直接通过 $z_n = \softmax(\frac{q_n K^T}{d_k}) V$ 的方式计算得到 $z_n$，然后预测下一个 token。

Hugging Face 对于 KV Cache 的实现代码如下：

```python
query, key, value = self.c_attn(hidden_states).split(self.split_size, dim=2)

query = self._split_heads(query, self.num_heads, self.head_dim)  # 当前token对应的query
key = self._split_heads(key, self.num_heads, self.head_dim)  # 当前token对应的key
value = self._split_heads(value, self.num_heads, self.head_dim)  # 当前token对应的value

if layer_past is not None:
    past_key, past_value = layer_past  # KV Cache
    key = torch.cat((past_key, key), dim=-2)  # 将当前token的key与历史的K拼接
    value = torch.cat((past_value, value), dim=-2)  # 将当前token的value与历史的V拼接

if use_cache is True:
    present = (key, value)
else:
    present = None

# 使用当前token的query与K和V计算注意力表示
if self.reorder_and_upcast_attn:
    attn_output, attn_weights = self._upcast_and_reordered_attn(query, key, value, attention_mask, head_mask)
else:
    attn_output, attn_weights = self._attn(query, key, value, attention_mask, head_mask)
```

KV Cache 是以空间换时间，当输入序列非常长的时候，需要缓存非常多 k 和 v，显存占用非常大。为了缓解该问题，可以使用 MQA、GQA、Page Attention 等技术。

# RoPE

为什么要位置编码？尽可能实现长序列上的无损外推

假设 $q_m$ 和 $q_n$ 表示位置 m 和位置 n 处的两个向量，如果不添加位置信息，则 $q_m \cdot q_n$ 不变，与希望距离越近关注越高不符合，加入位置信息解决这个问题。

对于 RoPE 而言，作者的出发点为：通过绝对位置编码的方式实现相对位置编码。即 RoPE 希望 $q_m \cdot q_n = f(q_m, q_n, m-n)$，m-n 表示相对位置信息。

为了简化问题，先假设词向量是二维

$$ q_m = R(m) q_m^0 $$

就是一个旋转矩阵

推广到多维，则可以把高维向量两两一组，分别旋转。

# Flash Attention

为什么提出：Transformers 计算长文本时平方时间复杂度 $O(n^2)$，很耗时很耗显存。有改进的方法降低计算复杂度，没太大用，原因缓存存取很耗时。FlashAttention 根据 SRAM 大小分块计算 softmax 优化了 MAC（存储访问开销），非优化计算复杂度。

# VLLM

通过 PagedAttention 高效地管理 attention 中缓存的张量。PagedAttention 灵感来自于操作系统中虚拟内存和分页的经典思想，它可以允许在非连续空间存储连续的 KV 张量。具体来说，PagedAttention 把每个序列的 KV 缓存进行了分块，每个块包含固定长度的 token，而在计算 attention 时可以高效地找到并获取那些块。

序列在分块之后，只有最后一个块可能会浪费内存（实际中浪费的内存低于 4%）。高效利用内存的好处很明显：系统可以在一个 batch 中同时输入更多的序列，提升 GPU 的利用率，显著地提升吞吐量。

PagedAttention 的另外一个好处是高效内存共享。例如，在并行采样的时候，一个 prompt 需要生成多个输出序列。这种情况下，对于这个 prompt 的计算和内存可以在输出序列之间共享。

# MLA

从其计算特征来看，同时解决了这两方面的问题：一方面，通过低秩压缩大幅降低了 KV Cache 的大小，另一方面，MLA 解压缩后的多头注意力机制能够提供较高的计算强度，有助于充分利用 GPU 的算力资源。

给定输入向量 $x$

在 DeepSeek-V2 中，Q 向量也采用了低秩压缩的方式。首先，将输入向量投影到一个 1536 维的低维空间：$c = W_{c} x$

然后，将其投影到 $d_h$ 的多头向量空间上（其中 H=128 是 heads 数），得到了 Q 向量的第一部分：$q_a = W_{q_a} c$

再将其投影到 $d_h$ 上并使用 RoPE 嵌入位置信息，得到 Q 向量的第二部分：$q_b = W_{q_b} c \odot \text{RoPE}$

将两部分拼接得到最终的 Q 向量：$q = [q_a; q_b]$

计算 KV 向量时，首先需要将输入向量投影为 512 维的联合压缩表示：$c^{kv} = W_{c^{kv}} x$

与 Q 向量的计算过程类似，K 向量的第一部分是将 $c^{kv}$ 投影到 $d_h$ 的多头向量空间：$k_a = W_{k_a} c^{kv}$

K 的第二部分是将输入向量投影到 64 维向量空间并施加 RoPE 嵌入位置信息：$k_b = W_{k_b} x \odot \text{RoPE}$

与 Q 不同的是，完整的 K 是将 K 的第二部分广播到每个 head 后与第一部分拼接得到：$k = [k_a; k_b]$，也就是说，每个 head 的 RoPE 部分是完全相同的。

V 向量的计算较为简单，直接将 $c^{kv}$ 解压缩到 $d_h$ 即可：$v = W_v c^{kv}$

## Attention 计算

Attention 的计算过程和传统的 MHA 并无差异。首先计算 attention score：$\text{score} = \frac{q k^T}{\sqrt{d_h}} \in \mathbb{R}^{H \times L}$

计算对 V 的加权和，并将所有 head 压平，得到 Attention 输出：$o = \softmax(\text{score}) v$

经过另一个矩阵的投影，就能得到 MLA 的最终输出：$y = W_o o$

相比缓存完整的 KV Cache，缓存压缩后的 KV Cache 会带来较大的性能下降。另外一个重要的问题是，当前的 CacheCompressed 实现实际上并不能缓解 KV Cache 过大的问题，这是由于在计算 MLA 的时候，仍然需要存储解压后的完整的 KV Cache，这很可能引起 OOM 崩溃。DeepSeek-V2 的论文中提出，可以将 KV 的解压缩矩阵吸收到 Q-projection 和 Out-projection 中，从而可以在不解压缩 KV Cache 的情况下直接计算最终的 Attention 结果。对于 K 的吸收，在 Attention Score 的计算公式中，非 RoPE 部分可以做如下展开：$q_a k_a^T = (W_{q_a} c) (W_{k_a} c^{kv})^T = c (W_{q_a}^T W_{k_a}) c^{kv}$

即通过矩阵乘法结合律，可以改为计算 $c (W_{q_a}^T W_{k_a}) c^{kv}$，避免了解压缩出完整的 K 矩阵。此外，在原始版本的解压缩的过程中，由于每个 token 的 key 都需要与 $W_{k_a}$ 相乘才能得到，因此计算量较大；矩阵吸收后，$W_{q_a}^T W_{k_a}$ 只需要对 $c^{kv}$ 这一个向量相乘，也大大减少了浮点计算量。

# 量化

## GPTQ

GPTQ 的思想最初来源于 Yann LeCun 在 1990 年提出的 OBD 算法，随后 OBS、OBC（OBQ）等方法不断进行改进，而 GPTQ 是 OBQ 方法的加速版。GPTQ 的量化有严谨的数学理论推导，所有的算法步骤都有理论支撑。

其中计算每个参数进行量化之后带来的损失。同时给出了一个参数更新大小，更新其余参数最小化该损失。

该方法的思路大致为：利用 hessian 信息作为准则判定每个权重量化后对输出 loss（通常定义为 MSE）造成的影响，量化影响最大的权重（即最敏感）挑选出来先进行量化，然后对其他权重进行更新来补偿该权重量化导致的影响，如此往复，直至全部量化结果。

## AWQ

该方法认为不是所有权重参数都是重要的，对模型结果影响最重要的可能占比只有 1%。

Note：指出 GPTQ 通过更新影响力较大的参数去弥补损失是得不偿失的，会使得性能下降。

AWQ 针对 channel 维度来寻找重要参数。在 AWQ 它判断这个 channel 是否是重要的依据是输入 X 以及这个参数本身 W 的绝对大小。在 AWQ 中它认为单独保存 fp16 效率太低，并且太复杂，所以它的方式是寻找一个缩放比例 s，在参数量化之前乘以这个比例，计算时输入 X 除以这个比例，以减小误差。

通过 grid search 得到每个 scale 的最优参数，该方案也是 4-bit weight-only quantization。
